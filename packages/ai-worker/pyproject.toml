[tool.poetry]
name = "ai-worker"
version = "0.1.0"
description = "AI Agent for CAD automation"
authors = ["Antigravity <antigravity@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
langchain = "^0.1.0"
google-generativeai = "^0.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
